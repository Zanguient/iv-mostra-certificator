
<% if(req.variables.messages.success){ %>
  <% req.variables.messages.success.forEach(function(msg){ %>
    <div class="alert alert-dismissable alert-success">
      <button type="button" class="close" data-dismiss="alert">×</button>
      <%= msg %>
    </div>
  <% }) %>
<% } %>

<% if(req.variables.messages.info){ %>

  <% req.variables.messages.info.forEach(function(msg){ %>
    <div class="alert alert-dismissable alert-info">
      <button type="button" class="close" data-dismiss="alert">×</button>
      <%= msg %>
    </div>
  <% }) %>
<% } %>

<% if(req.variables.messages.warning){ %>

  <% req.variables.messages.warning.forEach(function(msg){ %>
    <div class="alert alert-dismissable alert-warning">
      <button type="button" class="close" data-dismiss="alert">×</button>
      <%= msg %>
    </div>
  <% }) %>
<% } %>

<% if(req.variables.messages.error){ %>

  <% req.variables.messages.error.forEach(function(msg){ %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert">×</button>
      <%= msg %>
    </div>
  <% }) %>
<% } %>
